spring:

  datasource:
    url: jdbc:mysql://localhost:3306/logscanner?createDatabaseIfNotExist=true
    username: root
    password: Somil@123         # ← your MySQL password
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update             # auto creates tables
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: chotepyare1234@gmail.com        # ← replace
    password: tsjmskncpcsoiqkg      # ← use Gmail App Password


logging:
  file:
    name: logs/application.log            # ← Spring Boot creates this automatically
  level:
    root: INFO

app:
  log:
    file-path:  logs/application.log  # ← path to your log file
  mail:
    from: chotepyare1234@gmail.com                 # ← replace
    to: somiljain.cse25@jecrc.ac.in           # ← replace
  scan:
    cron: "0 */1 * * * *"                      # every 5 minutes